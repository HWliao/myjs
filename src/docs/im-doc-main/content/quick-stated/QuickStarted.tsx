import * as React from 'react';

export const QUICK_STARTED_TO = '/doc/quick_started';

class QuickStarted extends React.Component {
  constructor(props: {}) {
    super(props);
  }

  render() {
    return (
      <div>QuickStarted</div>
    );
  }
}

export default QuickStarted;
